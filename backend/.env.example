# SQLite is used by default — no database setup required.
# The database file (lost_and_found.db) is created automatically in the backend/ directory.

# No environment variables are required for the database.
# You can add other variables here if needed in the future.
